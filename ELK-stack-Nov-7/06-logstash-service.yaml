apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: elk-stack
spec:
  selector:
    app: logstash
  ports:
  - port: 5044
    name: beats
    targetPort: 5044
  - port: 5000
    name: tcp
    targetPort: 5000
  type: ClusterIP
